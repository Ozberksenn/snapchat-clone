import { StyleSheet } from "react-native";

export default StyleSheet.create({
  themeContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  Toggleicon: {
    fontSize: 130,
    color: "#000",
  },
});
